export const initialBlobityOptions = {
  licenseKey: "opensource",
  focusableElementsOffsetX: 4,
  focusableElementsOffsetY: 4,
  color: "#00FFFFFF",
  dotColor: "#08233b",
  
  invert: true,
  focusableElements:
    "[data-blobity], a:not([data-no-blobity]), button:not([data-no-blobity]), [data-blobity-tooltip]",
  font: "'Kumbh Sans', -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica Neue,Arial,sans-serif",
  fontSize: 16,
  fontWeight: 500,
  opacity: 1,
  fontColor: "#08233b",
  zIndex: 1000,
  magnetic: true,
  outline: true, // Bật chế độ outline
  outlineColor: "#E3D3BE", // Màu viền
  outlineWidth: 2, // Độ rộng viền
};
